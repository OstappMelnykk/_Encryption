@{
    ViewData["Title"] = "Home Page";
}


<h1>Before Encripting U should upload file!</h1>


<form asp-action="UploadFile" asp-controller="Caesar" method="post" enctype="multipart/form-data">
    <label for="file">Select a file:</label>
    <input type="file" id="fileInput" name="file">
    <br>
    <button type="submit">Upload</button>
</form>




<style>
    body {
        font-family: 'Arial', sans-serif;
        background-color: #f4f4f4;
        margin: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100vh;
    }

    h1 {
        color: #2c3e50;
        text-align: center;
    }

    form {
        background-color: #rgb(200, 200, 200);
        ;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        text-align: center;
    }

    label {
        display: block;
        margin-bottom: 10px;
        font-weight: bold;
        color: rgb(40, 41, 44);
    }

    #fileInput {
        display: block;
        width: 100%;
        padding: 10px;
        margin-bottom: 20px;
        box-sizing: border-box;
        border: 1px solid #bdc3c7;
        border-radius: 4px;
    }

    a, button {
        background-color: rgb(40, 41, 44);
        color: #fff;
        padding: 10px 20px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: background-color 0.3s ease-in-out;
    }

        a, button:hover {
            background-color: rgb(20, 21, 24);
        }

    a {
        margin: 30px;
        text-decoration: none;
        margin-top: 400px;
    }
</style>